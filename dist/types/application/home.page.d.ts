import React from 'react';
import { AppTabState } from '@application/AppCore';
interface AppTabProps {
    state: AppTabState;
}
declare function HomePage(props: AppTabProps): React.JSX.Element;
export default HomePage;
